{
  "name": "action-github-release",
  "version": "1.0.0",
  "description": "A GitHub Action to create releases",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No test specified\" && exit 0"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  }
}
